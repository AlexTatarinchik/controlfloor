{
    // This listens on all interfaces on port 8081
    listen: ":8081"
    
    https: false
    
    auth: {
        // "builtin" uses builtin database based auth
        // "mod" additionally checks for controlfloor_auth mod based auth
        type: "mod"
    }
}